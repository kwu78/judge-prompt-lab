# Cursor SDK orchestrator settings.
# Copy to .env.cursor and fill in values.
# The orchestrator reads both .env and .env.cursor, so ANTHROPIC_API_KEY
# can live in either file.

# Model used by the orchestrator agent.
# Defaults to claude-opus-4-8 for stronger reasoning over experiment state.
CURSOR_MODEL=claude-opus-4-8
